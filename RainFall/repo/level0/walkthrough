# Level 0 - аргумент

### 1. Открываем иду/гидру и видим, что нужно передать в аргументы бинаря число 423
```c
  if ( atoi(argv[1]) == 423 ) {
        ...

        execv("/bin/sh", v4);
  }

```

### 2. Передаем и получаем шелл
    ./level0 423
